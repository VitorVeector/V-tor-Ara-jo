import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
      primary: {
        main: '#f4f2ff',
      },
    },
});

export const inputProps = {
    style: { color: '#f4f2ff', fontFamily: 'Montserrat' }
};